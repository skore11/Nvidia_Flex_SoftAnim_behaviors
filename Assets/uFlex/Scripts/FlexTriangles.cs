using UnityEngine;
using System.Collections;


namespace uFlex
{
    public class FlexTriangles : MonoBehaviour
    {
        public int m_trianglesCount = 0;

        public int m_trianglesIndex = -1;

        // [HideInInspector]
        public int[] m_triangleIndices ; //!< Indexed triangle mesh for clothing

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
